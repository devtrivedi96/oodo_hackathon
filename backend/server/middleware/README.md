# Middleware

Express middleware used across server routes.

Known files:
- `authMiddleware.js` – protects routes by validating auth tokens/session

Middleware functions are mounted on routes to enforce policies.
