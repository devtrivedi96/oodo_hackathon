# Contexts

React context providers for app-wide state.

Known files:
- `AuthContext.tsx` – authentication state and helpers for the UI

Place context providers here to share state between components.
