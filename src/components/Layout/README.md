# Layout Components

Reusable layout elements for the app.

Files:
- `Navigation.tsx` – top navigation/menu
- `Sidebar.tsx` – application sidebar

Used by `App.tsx` to compose the overall UI structure.
