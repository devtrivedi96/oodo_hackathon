# Server Config

Server-specific configuration helpers.

Contents:
- `db.js` – database connection helper used by server components

This folder contains thin wrappers around DB/config used by controllers and routes.
