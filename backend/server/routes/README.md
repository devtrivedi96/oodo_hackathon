# Routes

Express route definitions mapping URLs to controllers.

Known files:
- `authRoutes.js` – authentication-related endpoints (login, register, OTP)

Check route files to see available API endpoints and middleware applied.
