# Components

React components grouped by feature and layout.

Subfolders (feature areas):
- `Analytics/`, `Auth/`, `Dashboard/`, `Drivers/`, `Expenses/`, `Layout/`, `Maintenance/`, `Trips/`, `Vehicles/`

Each subfolder contains related component files (forms, lists, pages).
