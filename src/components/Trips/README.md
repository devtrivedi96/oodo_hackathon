# Trips Components

Dispatcher and trip forms for trip management.

Files:
- `TripDispatcher.tsx` – dispatching UI for trips
- `TripForm.tsx` – create/edit trip form

Used to schedule and manage trips within the system.
