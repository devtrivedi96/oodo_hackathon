# Backend Config

Configuration helpers for the backend.

Contents:
- `database.js` – DB connection/config helper used by the backend

This folder centralizes environment-specific DB configuration.
