# Auth Components

Authentication UI: login, registration and OTP verification.

Files:
- `Login.tsx` – login form
- `Register.tsx` – registration form
- `VerifyOTP.tsx` – OTP verification flow

These components integrate with `AuthContext` and backend auth routes.
