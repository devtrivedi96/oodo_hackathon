# Controllers

Request handlers for server routes.

Known files:
- `authController.js` – handles authentication flows (login, register, OTP verify)

Controllers implement business logic and call DB/config helpers.
